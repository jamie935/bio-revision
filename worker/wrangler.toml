name = "bio-revision-pipeline"
main = "src/index.ts"
compatibility_date = "2024-12-01"

[vars]
ALLOWED_ORIGIN = "https://jamie935.github.io"
GITHUB_REPO = "jamie935/bio-revision"

# Secrets (set via: npx wrangler secret put GEMINI_API_KEY)
# Secrets (set via: npx wrangler secret put GITHUB_PAT)
